PRODUCTION_SERVER_TYPE=serverless or traditional
NODE_ENV=development or production
PORT=3000
AUTH_SECRET=your_secret
CLIENT_ID=your_google_oauth_client_id
CLIENT_SECRET=your_google_oauth_client_secret
DATABASE_URL=your_postgres_url
ACCESS_KEY_ID=your_s3_access_key
SECRET_ACCESS_KEY=your_s3_secret_key
# Use a full URL for non-AWS providers (e.g., MinIO): http://localhost:9000
# For production (Tabi.io) you'll typically use an https endpoint, e.g. https://s3.tabi.io
S3_ENDPOINT=http://localhost:9000
S3_REGION=your_s3_region
S3_BUCKET=your_s3_bucket

# Example Tabi.io production values (uncomment and set in Vercel):
# ACCESS_KEY_ID=your_tabi_access_key
# SECRET_ACCESS_KEY=your_tabi_secret
# S3_ENDPOINT=https://s3.tabi.io
# S3_REGION=auto-or-specific-region
# S3_BUCKET=your_bucket_name